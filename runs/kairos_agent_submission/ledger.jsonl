{"iteration": 1, "hypothesis": {"statement": "Stop feeding calibrated scores into a downstream tree. Switch to train_cfg mode='scores': inside build(), produce several INDEPENDENT ranker outputs (ctx.refit_score, ctx.din_score, ctx.baseline_score, ctx.cf_score, and an MF dot-product from ctx.mf_factors), each seed-averaged where cheap, and combine them ONLY at the output level by within-user rank fusion (per-user percentile rank of each member, weighted average). No single LightGBM ever sees a calibrated score as an input column.", "mechanism": "The three failed runs all had the same defect: a smooth, monotone, already-well-calibrated score was passed as a leaf-splitting feature, so the tree replaced a continuous within-user ordering with ~30 step levels, destroying the fine-grained intra-list ordering that GAUC measures. Rank fusion never quantises: it maps each member's score to its exact within-user percentile and averages, so ordering information is preserved exactly and the only thing that changes is the relative weight of decorrelated error directions. ctx.refit_score (refit FM-style ID crosses) and ctx.din_score (attention over history) make different mistakes on the same list; cf_score and MF add a third, collaborative error direction. Averaging their per-user ranks cancels member-specific list-level noise, which is precisely the noise GAUC integrates over the whole list, so GAUC should move before nDCG@5 does.", "predicted_effect": "GAUC rises materially (the full-list pairwise ordering is what averaging decorrelated ranks repairs); nDCG@5 rises less, since top-5 placement is dominated by the strongest single member. Per-member vs fused correlation should show fused rank correlating ~0.9 with each member and beating all of them individually. Expect primary ~0.604-0.606, i.e. at or above the +0.0030 the hand-run achieved, because we add cf/MF members on top of the two-model blend.", "predicted_gain": 0.004, "family": "ensemble", "source": "agent", "prediction": {"diagnostic": "gauc", "direction": "increase"}}, "action_kind": "patch", "code_diff": "def build(ctx):\n    import numpy as np\n\n    n = ctx.data.n\n    uid = ctx.data.user_id\n\n    def within_user_rank(score):\n        score = np.asarray(score, dtype=np.float64)\n        order = np.argsort(uid, kind='stable')\n        inv = np.empty_like(order)\n        inv[order] = np.arange(n)\n        sorted_uid = uid[order]\n        sorted_score = score[order]\n\n        ranks = np.empty(n, dtype=np.float64)\n        boundaries = np.nonzero(np.diff(sorted_uid))[0] + 1\n        starts = np.concatenate(([0], boundaries))\n        ends = np.concatenate((boundaries, [n]))\n\n        for s, e in zip(starts, ends):\n            seg = sorted_score[s:e]\n            m = e - s\n            if m == 1:\n                ranks[s:e] = 0.5\n                continue\n            order2 = np.argsort(seg, kind='stable')\n            r = np.empty(m, dtype=np.float64)\n            r[order2] = np.arange(m)\n            # average ties\n            uniq_vals, inv_idx, counts = np.unique(seg, return_inverse=True, return_counts=True)\n            sum_r = np.zeros(len(uniq_vals))\n            np.add.at(sum_r, inv_idx, r)\n            avg_r = sum_r / counts\n            r_final = avg_r[inv_idx]\n            ranks[s:e] = (r_final + 0.5) / m\n\n        out = np.empty(n, dtype=np.float64)\n        out[order] = ranks\n        return out\n\n    m1 = np.asarray(ctx.refit_score(), dtype=np.float64)\n    m2 = np.asarray(ctx.din_score(), dtype=np.float64)\n    m3 = np.asarray(ctx.baseline_score, dtype=np.float64)\n    cf_score, hist_count = ctx.cf_score()\n    m4 = np.asarray(cf_score, dtype=np.float64)\n\n    U, V = ctx.mf_factors(dim=16)\n    m5 = np.sum(np.asarray(U, dtype=np.float64) * np.asarray(V, dtype=np.float64), axis=1)\n\n    r1 = within_user_rank(m1)\n    r2 = within_user_rank(m2)\n    r3 = within_user_rank(m3)\n    r4 = within_user_rank(m4)\n    r5 = within_user_rank(m5)\n\n    s = 0.35 * r1 + 0.30 * r2 + 0.20 * r3 + 0.10 * r4 + 0.05 * r5\n\n    X = s.reshape(-1, 1).astype(np.float32)\n    names = ['fused_rank_score']\n    train_cfg = {'mode': 'scores'}\n\n    ctx.check(X, names)\n    return X, names, train_cfg", "outcome": {"valid_primary": 0.6033507249049987, "valid_gauc": 0.6698438025543539, "valid_ndcg": 0.5368576472556436, "delta_vs_incumbent": 0.001750724904998724, "prediction_hit": null, "diagnostics": {"seed_std": 0.0, "seeds": [0.6033507249049987], "audit": [{"check": "staleness_parity", "severity": "WARN", "detail": "no staleness column: the model cannot discount aged history", "value": null}]}, "seconds": 41.3}, "decision": "accept", "reason": "valid 0.6034 +-0.0000 vs incumbent 0.6016 (delta +0.0018)", "errors": [], "budget": {"tokens_in": 6614, "tokens_out": 2762, "wall_clock_s": 41.3, "iterations_used": 1}, "interventions": 0, "t": "2026-08-30 21:59:15"}
{"iteration": 2, "hypothesis": {"statement": "Build a multi-signal blend matrix: all decorrelated model scores (ctx.baseline_score, ctx.refit_score, ctx.din_score, ctx.cf_score, ctx.auxiliary_signal, ctx.mf_factors) PLUS a per-user (within eval-list) rank-percentile and z-score transform of each signal, and let LightGBM learn the combination on those normalised columns rather than the raw scales.", "mechanism": "Two effects stack. (1) Decorrelation: baseline_score carries the FM ID crosses, din_score an attention/architecture-different view, cf_score/mf_factors collaborative structure, auxiliary_signal a different feedback channel; averaging errors that are not shared is the only intervention measured to work here, and the previous iteration already got +0.0018 from a partial version of this. (2) Scale alignment: raw scores from different models have per-user offsets and spreads that differ (a user whose FM logits sit in a narrow band vs one with a wide band). LightGBM splits on GLOBAL thresholds, so a raw-score blend implicitly weights signals differently for different users and mostly wastes tree capacity on inter-user level differences, which are metric-irrelevant (within-user constants cannot move GAUC/nDCG). Converting each signal to its within-user percentile rank and per-user z-score removes exactly the metric-irrelevant part and makes one global split threshold mean the same thing for every user, so the tree spends its capacity on the within-list ordering that the metric actually reads. Disagreement features (std across the signals' rank-percentiles) additionally tell the tree when to fall back to the strongest single signal.", "predicted_effect": "The per-user normalisation pays off most where a list has many items to order and where raw-score offsets do the most damage: the long eval-list slice (eval_list_size bucket 12, 2352 users, currently auc_mean 0.6758 with by far the largest headroom 0.0705). I expect auc_long_lists to rise by ~0.008-0.015, with GAUC up ~0.005 overall and the popularity/duration inversion losses falling slightly as cf/mf supply item-side ordering the FM crosses miss.", "predicted_gain": 0.0045, "family": "ensemble", "source": "agent", "prediction": {"diagnostic": "auc_long_lists", "direction": "increase"}}, "action_kind": "patch", "code_diff": "def build(ctx):\n    import numpy as np\n    n = ctx.data.n\n    uid = ctx.data.user_id\n\n    baseline = ctx.refit_score()\n    din = ctx.din_score()\n    cf, cf_hist = ctx.cf_score()\n    U, V = ctx.mf_factors(dim=16)\n    mf_dot = np.sum(U * V, axis=1).astype(np.float32)\n\n    aux_names = ['is_click', 'is_like', 'is_follow', 'is_comment', 'is_forward']\n    aux = [ctx.auxiliary_signal(a) for a in aux_names]\n\n    signals = [baseline, din, cf, mf_dot] + aux\n    sig_names = ['baseline', 'din', 'cf', 'mf_dot'] + aux_names\n\n    # group by user\n    order = np.argsort(uid, kind='stable')\n    inv = np.empty(n, dtype=np.int64)\n    inv[order] = np.arange(n)\n    sorted_uid = uid[order]\n    # find group boundaries\n    change = np.empty(n, dtype=bool)\n    change[0] = True\n    change[1:] = sorted_uid[1:] != sorted_uid[:-1]\n    group_id_sorted = np.cumsum(change) - 1\n    group_id = np.empty(n, dtype=np.int64)\n    group_id[order] = group_id_sorted\n    n_groups = group_id_sorted[-1] + 1\n\n    # group counts\n    group_size = np.bincount(group_id, minlength=n_groups).astype(np.float32)\n    n_group_row = group_size[group_id]\n\n    feats = []\n    names = []\n\n    feats.append(baseline.astype(np.float32)); names.append('baseline_score')\n    feats.append(din.astype(np.float32)); names.append('din_score')\n    feats.append(cf.astype(np.float32)); names.append('cf_score')\n    feats.append(cf_hist.astype(np.float32)); names.append('cf_hist_count')\n    feats.append(mf_dot); names.append('mf_dot')\n    for a, an in zip(aux, aux_names):\n        feats.append(a.astype(np.float32)); names.append('aux_' + an)\n\n    feats.append(n_group_row.astype(np.float32)); names.append('n_group')\n\n    pct_list = []\n    for s, sn in zip(signals, sig_names):\n        s = s.astype(np.float64)\n        # sort within group using group_id then value\n        sort_key = np.lexsort((s, group_id))\n        ranks = np.empty(n, dtype=np.float64)\n        # rank within group: position within sorted group block\n        # compute using group boundaries on sort_key order\n        gs = group_id[sort_key]\n        pos_in_group = np.empty(n, dtype=np.float64)\n        # start index of each group in sort_key order\n        change2 = np.empty(n, dtype=bool)\n        change2[0] = True\n        change2[1:] = gs[1:] != gs[:-1]\n        start_idx = np.where(change2)[0]\n        group_start_for_pos = np.zeros(n, dtype=np.int64)\n        # cumulative count within group\n        counter = np.arange(n) - np.repeat(start_idx, np.diff(np.append(start_idx, n)))\n        pos_in_group[sort_key] = counter\n        denom = np.maximum(n_group_row - 1, 1.0)\n        pct = pos_in_group / denom\n        pct[n_group_row <= 1] = 0.5\n\n        # mean/std within group\n        sum_s = np.bincount(group_id, weights=s, minlength=n_groups)\n        mean_s = sum_s / np.maximum(group_size, 1)\n        sum_s2 = np.bincount(group_id, weights=s * s, minlength=n_groups)\n        var_s = sum_s2 / np.maximum(group_size, 1) - mean_s ** 2\n        var_s = np.maximum(var_s, 0)\n        std_s = np.sqrt(var_s)\n        mean_row = mean_s[group_id]\n        std_row = std_s[group_id]\n        z = (s - mean_row) / (std_row + 1e-6)\n\n        # max within group\n        max_s = np.zeros(n_groups, dtype=np.float64)\n        np.maximum.at(max_s, group_id, s)\n        max_row = max_s[group_id]\n        gap = s - max_row\n\n        feats.append(pct.astype(np.float32)); names.append('pct_' + sn)\n        feats.append(z.astype(np.float32)); names.append('z_' + sn)\n        feats.append(gap.astype(np.float32)); names.append('gap_' + sn)\n        pct_list.append(pct)\n\n    pct_arr = np.stack(pct_list, axis=1)\n    mean_pct = pct_arr.mean(axis=1)\n    disagree = pct_arr.std(axis=1)\n    feats.append(mean_pct.astype(np.float32)); names.append('mean_pct')\n    feats.append(disagree.astype(np.float32)); names.append('disagree_pct')\n    feats.append((pct_list[0] - mean_pct).astype(np.float32)); names.append('baseline_minus_consensus')\n\n    X = np.stack(feats, axis=1).astype(np.float32)\n    ctx.check(X, names)\n    train_cfg = {'objective': 'binary', 'group': 'user_day', 'hparams': {}}\n    return X, names, train_cfg\n", "outcome": {"valid_primary": 0.5999776429218208, "valid_gauc": 0.6655270450266513, "valid_ndcg": 0.5344282408169903, "delta_vs_incumbent": -0.0033730819831779835, "prediction_hit": false, "diagnostics": {"seed_std": 0.0002577692840056718, "seeds": [0.6000940733655092, 0.6002185939906948, 0.5996202614092584], "audit": [{"check": "staleness_parity", "severity": "WARN", "detail": "no staleness column: the model cannot discount aged history", "value": null}]}, "seconds": 174.6}, "decision": "reject", "reason": "valid 0.6000 +-0.0003 vs incumbent 0.6034 (delta -0.0034)", "errors": [], "budget": {"tokens_in": 13581, "tokens_out": 7151, "wall_clock_s": 215.9, "iterations_used": 2}, "interventions": 0, "t": "2026-08-30 22:02:10"}
{"iteration": 3, "hypothesis": {"statement": "Consolidating (not swinging): keep the incumbent's score-blending idea but make it complete and within-user-normalised. Return a matrix whose columns are ALL the decorrelated model signals (ctx.baseline_score, ctx.refit_score, ctx.din_score, ctx.cf_score, ctx.auxiliary_signal, and the user-item dot/elementwise summary of ctx.mf_factors) PLUS, for each of them, three per-user (per evaluation list) normalised transforms: within-list rank/(n-1), within-list z-score, and value minus within-list mean. LightGBM then learns the blend on comparable within-list scales instead of on raw absolute scores.", "mechanism": "The metric is invariant to per-user constants, so only the within-list ORDERING information in each score matters. Raw scores from different architectures have user-dependent offsets and scales (and the 5x logging-density collapse shifts those offsets between train and valid/test), so a tree that splits on an absolute threshold of baseline_score mixes users whose whole list sits at different score levels and effectively wastes splits on per-user constants. Rank/z-normalising each signal inside the evaluation list removes that user-level offset, making every split a genuine within-list comparison, and lets the tree combine several decorrelated signals (CF, auxiliary feedback, DIN, refit) on a common scale - i.e. a learned, list-aware ensemble rather than a raw-score ensemble. The distortion from absolute thresholds grows with list length (more items spanned by one offset), so the benefit should concentrate in the long-list slice, which is also where the headroom is largest (eval_list_size bucket 12: headroom 0.0705 vs ~0.018 elsewhere).", "predicted_effect": "GAUC rises mainly through the long-list users; AUC in the largest eval_list_size bucket (bucket 12, currently 0.6758) should gain more than the short-list buckets, and overall primary should move above 0.6055.", "predicted_gain": 0.0032, "family": "ensemble", "source": "agent", "prediction": {"diagnostic": "auc_long_lists", "direction": "increase"}}, "action_kind": "patch", "code_diff": "def build(ctx):\n    import numpy as np\n\n    n = ctx.data.n\n    user_id = ctx.data.user_id\n\n    baseline = ctx.refit_score()\n    din = ctx.din_score()\n    cf_score, cf_hist = ctx.cf_score()\n    aux_click = ctx.auxiliary_signal('is_click')\n    aux_like = ctx.auxiliary_signal('is_like')\n\n    U, V = ctx.mf_factors(dim=16)\n    mf_dot = np.sum(U * V, axis=1).astype(np.float32)\n\n    base_cols = {\n        'baseline': baseline.astype(np.float32),\n        'din': din.astype(np.float32),\n        'cf': cf_score.astype(np.float32),\n        'aux_click': aux_click.astype(np.float32),\n        'aux_like': aux_like.astype(np.float32),\n        'mf_dot': mf_dot.astype(np.float32),\n    }\n\n    # group by user_id\n    order = np.argsort(user_id, kind='stable')\n    inv_order = np.empty_like(order)\n    inv_order[order] = np.arange(n)\n\n    sorted_uid = user_id[order]\n    # boundaries of groups\n    change = np.empty(n, dtype=bool)\n    change[0] = True\n    change[1:] = sorted_uid[1:] != sorted_uid[:-1]\n    group_start = np.flatnonzero(change)\n    group_end = np.append(group_start[1:], n)\n\n    group_id = np.cumsum(change) - 1  # group index per sorted position\n    n_groups = group_start.size\n\n    group_size_sorted = (group_end - group_start)[group_id]\n\n    feats = []\n    names = []\n\n    for cname, col in base_cols.items():\n        sorted_col = col[order]\n\n        # rank within group\n        rank_sorted = np.empty(n, dtype=np.float32)\n        mean_sorted = np.empty(n, dtype=np.float32)\n        std_sorted = np.empty(n, dtype=np.float32)\n        max_sorted = np.empty(n, dtype=np.float32)\n\n        for g in range(n_groups):\n            s, e = group_start[g], group_end[g]\n            seg = sorted_col[s:e]\n            order_seg = np.argsort(seg, kind='stable')\n            ranks = np.empty(len(seg), dtype=np.float32)\n            ranks[order_seg] = np.arange(len(seg), dtype=np.float32)\n            denom = max(len(seg) - 1, 1)\n            rank_sorted[s:e] = ranks / denom\n            m = seg.mean()\n            sd = seg.std()\n            mx = seg.max()\n            mean_sorted[s:e] = m\n            std_sorted[s:e] = sd\n            max_sorted[s:e] = mx\n\n        z_sorted = (sorted_col - mean_sorted) / (std_sorted + 1e-6)\n        centered_sorted = sorted_col - mean_sorted\n        maxdiff_sorted = sorted_col - max_sorted\n\n        rank_col = np.empty(n, dtype=np.float32)\n        z_col = np.empty(n, dtype=np.float32)\n        centered_col = np.empty(n, dtype=np.float32)\n        maxdiff_col = np.empty(n, dtype=np.float32)\n\n        rank_col[order] = rank_sorted\n        z_col[order] = z_sorted\n        centered_col[order] = centered_sorted\n        maxdiff_col[order] = maxdiff_sorted\n\n        feats.append(col)\n        names.append(cname)\n        feats.append(rank_col)\n        names.append(cname + '_rank')\n        feats.append(z_col)\n        names.append(cname + '_z')\n        feats.append(centered_col)\n        names.append(cname + '_centered')\n        feats.append(maxdiff_col)\n        names.append(cname + '_maxdiff')\n\n    group_size_col = np.empty(n, dtype=np.float32)\n    group_size_col[order] = np.log1p(group_size_sorted).astype(np.float32)\n    feats.append(group_size_col)\n    names.append('group_size_log')\n\n    X = np.stack(feats, axis=1).astype(np.float32)\n\n    ctx.check(X, names)\n\n    train_cfg = {\n        'objective': 'binary',\n        'group': 'user',\n        'hparams': {}\n    }\n\n    return X, names, train_cfg\n", "outcome": {"valid_primary": 0.6006535811515127, "valid_gauc": 0.6663965283548228, "valid_ndcg": 0.5349106339482028, "delta_vs_incumbent": -0.0026971437534860643, "prediction_hit": false, "diagnostics": {"seed_std": 0.0001521908653278821, "seeds": [0.6004514839986118, 0.6006905154173465, 0.60081874403858], "audit": [{"check": "staleness_parity", "severity": "WARN", "detail": "no staleness column: the model cannot discount aged history", "value": null}]}, "seconds": 160.2}, "decision": "reject", "reason": "valid 0.6007 +-0.0002 vs incumbent 0.6034 (delta -0.0027)", "errors": [], "budget": {"tokens_in": 24117, "tokens_out": 12373, "wall_clock_s": 376.1, "iterations_used": 3}, "interventions": 0, "t": "2026-08-30 22:04:50"}
