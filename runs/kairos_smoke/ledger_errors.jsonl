{"iteration": 1, "kind": "temporal_validity", "detail": "'user_rate' varies within a user's valid list (max dev 1.24e-01). A user-level statistic can only vary if it absorbed labels from inside the evaluation window.; 'user_logn' varies within a user's valid list (max dev 3.09e+00). A user-level statistic can only vary if it absorbed labels from inside the evaluation window.; 'user_rate' varies within a user's test list (max dev 5.61e-03). A user-level statistic can only vary if it absorbed labels from inside the evaluation window.; 'user_logn' varies within a user's test list (max dev 1.18e-02). A user-level statistic can only vary if it absorbed labels from inside the evaluation window.", "recovery": "rejected before it could be believed; the specific violation was returned to the proposer for rewrite", "t": "23:54:39"}
